# Eclat POS — Test Plan (Cambios del 19 de mayo)

> **Alcance**: tests específicos para los 15 commits del 19 de mayo (puntos 2, 4, 6, 10, 11, 12, 13 del changelog + post-audit fixes). Complementa `TEST-PLAN.md` (que cubre todo el sistema).
>
> **Legend**: ☐ = Pending | ✅ = Passed | ❌ = Failed | ⚠️ = Partial/Known issue | ⏭️ = Skipped
>
> **Roles**: 🖥️ = UI/UX (manual / Playwright) | ⚙️ = Backend (Claude — code audit, RPCs, SQL)
>
> **Datos de prueba sugeridos**: Cliente "MARIA DEL ROSARIO CORTEZ BORCEGUIN" (V-0047 con descuento 99.99%), Cliente "CECILIA JOSEFINA VARGAS HERNANDEZ" (V-0048 con regalo + descuento), Rimel (producto multi-variante: "De Luxe" + "Roll-On").

## Run · 22 may 2026 (Claude)

- **P0 críticos** (9/9): code audit + UI runtime — 3.10, 3.11, 8.11, 10.16, 11.6, 11.9, 11.11, 11.13, 13.1
- **Secciones cerradas**: 1, 2, 4, 5, 6, 7, 8 vía mezcla de code audit + Playwright + Supabase SQL
- **Pendiente**: sections 3 (resto), 9, 10 (resto), 11 (resto), 12, 13 (resto)
- **Evidencia**: screenshots en `/` raíz (`networks-tab.png`, `customer-edit-dialog.png`, `pending-wizard-2.png`, `v0047-detail.png`, `.playwright-mcp/Recibo-V-0047.pdf` + render `.playwright-mcp/Recibo-V-0047-1.png`)

## Run · 23 may 2026 (Claude + usuario)

- **Bug encontrado y corregido**: `2.6` / nuevo `2.13` — click en "+" de producto sin stock no abría el wizard (mostraba palomita verde pero `handleAddProduct` hacía `return` temprano). Fix en `pos-landing.tsx`: usar `activeVariants` + diálogo de confirmación OOS→vale, replicando `wizard-products-step`. Verificado manual por el usuario.
- **2.6 cerrado** (era ⚠️) tras verificar el "+" directo. Total UI 115→116.
- **8.12 cerrado** (era ⚠️): toast de red duplicada confirmado manual. No era bug — solo no se había capturado en el run anterior.
- **Parciales restantes: 0** (ambos ⚠️ resueltos).
- **Batch code-audit/SQL cerrado** (20 pruebas, sin app): 3.4–3.6, 9.1–9.4, 10.2–10.7, 12.1, 13.2–13.7 → todas ✅ con evidencia `file:line`. Las que quedan pendientes requieren la app corriendo (UI/Playwright) o hardware (10.15 impresión térmica).

---

## Table of Contents

1. [Variant Sorting — expansión a 6 nuevos lugares](#1-variant-sorting)
2. [POS Landing — sliding cart removido + auto-open wizard](#2-pos-landing)
3. [POS Sale Wizard — pending discount + confirm-on-discard](#3-pos-sale-wizard)
4. [POS Variant Picker en Landing](#4-pos-variant-picker)
5. [`/ventas` KPIs reactivos al filtro de fecha](#5-ventas-kpis)
6. [Inventario Hub — `grand_total` excluye carga inicial](#6-inventario-hub)
7. [Customer Networks — DB, RPC atómico, RLS](#7-customer-networks-backend)
8. [Customer Networks — UI (NetworkManager + form + tabla)](#8-customer-networks-ui)
9. [Recibos — Paridad de descuentos per-producto](#9-recibos-paridad)
10. [Recibo Térmico — B&W monospace + logos](#10-recibo-termico)
11. [Recibo PDF — Tamaño carta + overflow + italic fix](#11-recibo-pdf)
12. [Cross-feature integration](#12-cross-feature)
13. [Edge cases & regressions](#13-edge-cases)

---

## 1. Variant Sorting

> Commit `8de74da` + extensión en `91a0278`. `sortVariantsBySku` movido a `@/lib/utils`. Aplicado en 7 lugares.

### ⚙️ Backend — Code audit

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 1.1 | `sortVariantsBySku` usa `localeCompare` con `numeric: true` | "E-99" < "E-100" (orden natural) | ✅ | `src/lib/utils.ts:48` `numeric: true, sensitivity: "base"` |
| 1.2 | Variantes sin SKU (`null`) van al final | Sort ascendente coloca `""` al inicio o final consistentemente | ✅ | `a.sku ?? ""` ordena empty al inicio (asc) |
| 1.3 | Re-export en `src/features/pos/utils.ts` mantiene compat | Imports antiguos siguen funcionando | ✅ | `src/features/pos/utils.ts:3` |

### 🖥️ UI/UX — Listados con variantes

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 1.4 | `/productos` list-view → expand "Rimel" → variantes ordenadas por SKU | "De Luxe" (M-3117) antes que "Roll-On" (N-3102)... actualmente alfabético por SKU | ✅ | `product-list-view.tsx:62` |
| 1.5 | `/productos` grid-card → expand "Rimel" → ordenado por SKU | Mismo orden que 1.4 | ✅ | `product-card.tsx:73` |
| 1.6 | POS search → click "Rimel" → variantes en variant-picker ordenadas | Picker abre con SKUs en orden | ✅ | `product-search.tsx:174` |
| 1.7 | POS wizard → step Productos → click "Rimel" → variant picker ordenado | Mismo orden | ✅ | `wizard-products-step.tsx:615` |
| 1.8 | Bundle manager (productos → editar cofre → agregar componente) → variantes ordenadas | SKUs en orden natural | ✅ | `bundle-manager.tsx:177` |
| 1.9 | Transit picker (`/inventario/transito` → agregar producto) → ordenado | SKUs en orden | ✅ | `transit-product-picker.tsx:189` |
| 1.10 | Crear nota de crédito (`/notas-credito` → +) → variantes en lista ordenadas | SKUs en orden | ✅ | `create-credit-note-dialog.tsx:384` |
| 1.11 | Customer price editor (`/configuracion` → descuentos → editar lista) → search variantes ordenadas | Resultados en orden natural | ✅ | `customer-price-editor.tsx:77` |

---

## 2. POS Landing

> Commit `91a0278`. Sliding cart eliminado, `WizardMode = "from-cart"` removido, agregar producto auto-abre wizard, cerrar wizard limpia carrito.

### ⚙️ Backend — Code audit

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 2.1 | `pos-sliding-cart.tsx` no existe en filesystem | `ls` confirma archivo borrado | ✅ | `ls` no matches |
| 2.2 | Búsqueda `POSSlidingCart`, `POSCartFAB`, `"from-cart"` en src/ → 0 resultados | grep -r limpio | ✅ | grep limpio |
| 2.3 | `WizardMode` type tiene exactamente `"new-sale" \| "complete-pending"` | TS type check | ✅ | `pos-landing.tsx:33`, `pos-sale-wizard.tsx:31` |
| 2.4 | `STEPS_BY_MODE` solo tiene 2 entradas | new-sale (4 steps) + complete-pending (2 steps) | ✅ | `pos-sale-wizard.tsx:42-45` |

### 🖥️ UI/UX — Flujo nuevo

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 2.5 | Cargar `/pos` con carrito vacío | NO se ve panel de carrito a la derecha | ✅ | Snapshot landing sin sliding panel |
| 2.6 | Click "+" en producto de 1 variante (e.g. Acido Hialuronico) | Producto se agrega + wizard auto-abre en paso "Cliente" | ✅ | Verificado manual 23-may con click directo en "+" (no solo CTA) |
| 2.7 | Wizard abierto → step "Productos" muestra el producto que se agregó del landing | Item en panel derecho del wizard con qty 1 | ✅ | V-0062 step Confirmar: 1× Acido Hialuronico |
| 2.8 | Click "Nueva venta" en hero (PageHero) | Wizard abre vacío en paso "Cliente" (mismo modo "new-sale") | ✅ | Probado durante V-0062 |
| 2.9 | Cerrar wizard (X) sin completar venta | Carrito se limpia | ✅ | Playwright 23-may: ESC→Descartar; luego "Nueva venta"+ESC cerró sin confirm → carrito vacío |
| 2.10 | Tras cerrar, agregar otro producto desde landing | Wizard re-abre con carrito de 1 item (no stacked) | ✅ | Playwright 23-may: tras descartar Acido, agregar Rimel → carrito solo 1 item (Rimel ×1), no apilado |
| 2.11 | Carrito persiste entre paso Cliente y paso Productos del wizard | Producto agregado del landing visible en step 2 | ✅ | V-0062 flow consistente |
| 2.12 | `/pos?action=new` URL param sigue funcionando | Wizard auto-abre vacío | ✅ | `pos-landing.tsx` useEffect activo |
| 2.13 | Click "+" en producto SIN stock (candidato a vale) | Diálogo "Producto sin stock" → "Entendido, agregar" → wizard abre con el item como vale | ✅ | **Fix 23-may**: `handleAddProduct` filtraba por `stock > 0` → `return` temprano (no agregaba ni abría wizard, pero la palomita verde aparecía igual). Ahora usa `activeVariants` + diálogo OOS, replicando `wizard-products-step`. Bonus: corregido cofre sin `bundleComponents` al agregar desde landing. Verificado manual 23-may |

---

## 3. POS Sale Wizard

> Commits `91a0278` (pending discount) + `499f9ed` (confirm-on-discard) + `9fae249` (discount_percent propagation).

### ⚙️ Backend — Code audit

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 3.1 | `WizardPaymentStep` recibe prop `pendingSaleDiscount?: { amount, percent, subtotal }` | TypeScript types correctos | ✅ | `wizard-payment-step.tsx:71` |
| 3.2 | `pendingSaleDiscount` solo se pasa cuando `mode === "complete-pending"` | Condicional en wizard | ✅ | `pos-sale-wizard.tsx:696` ternary |
| 3.3 | En modo pendiente, `setCartDiscount` NO se puede llamar (botones ocultos) | Branch `!isPendingMode` en todos los controles | ✅ | `wizard-payment-step.tsx:187, 206, 218` |
| 3.4 | `usePendingSales` query incluye `discount_percent` en select | Verificar `sale_items(...discount_percent...)` | ✅ | `queries.ts:422,424` top-level + `sale_items(...discount_percent...)`, map en 439 |
| 3.5 | `PendingSaleWithSummary.items[].discount_percent` existe en tipo | TS check | ✅ | `types.ts:69` (sale) + `:81` (items) `number \| null` |
| 3.6 | `buildReceiptSnapshot` propaga `i.discount_percent` en branch complete-pending | NO usa `null` hardcoded | ✅ | `pos-sale-wizard.tsx:141` `i.discount_percent ?? null` + `:147` pendingSale |
| 3.7 | `handleClose` muestra ConfirmDialog cuando `items.length > 0 && !saleResult && mode !== "complete-pending"` | Condicional verificado | ✅ | `pos-sale-wizard.tsx:556` |

### 🖥️ UI/UX — Pending sale read-only discount

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 3.8 | Crear venta nueva con cliente + descuento 30% + 1 producto → "Confirmar después" → guarda pendiente | Pendiente V-XXXX creada | ✅ | V-0062 creada con Acido + 30% (Distribuidor Principal) |
| 3.9 | Reabrir pendiente desde `/pos` → click "Completar pago" → paso Pago | Banner "Descuento aplicado (30%) -$XXX" visible | ✅ | Banner "(30%) -$207.00" visible (screenshot) |
| 3.10 | En paso Pago de pendiente, NO debe aparecer botón "Agregar descuento" | Botón oculto (era el bug del punto 2) | ✅ | Screenshot `pending-wizard-2.png` |
| 3.11 | En paso Pago de pendiente, NO debe aparecer botón X para quitar descuento | X oculto (read-only) | ✅ | Banner sin X (screenshot) |
| 3.12 | Total a cobrar = total guardado de la pendiente, no del store | Si el store está vacío sigue mostrando total correcto | ✅ | Total $483.00 = $690 − $207 |
| 3.13 | Completar pendiente y descargar PDF → muestra `discount_percent` real per-item | No es valor calculado por aproximación | ✅ | Composición: 3.6 (`buildReceiptSnapshot` usa `i.discount_percent ?? null` en branch complete-pending) + 9.12 (PDF renderiza discount_percent per-item real, demostrado con V-0048). No se mutó la DB |

### 🖥️ UI/UX — Confirm-on-discard

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 3.14 | Wizard abierto sin items + ESC | Cierra inmediato sin confirm | ✅ | Playwright 23-may: "Nueva venta" vacío + ESC → cierra sin confirm |
| 3.15 | Wizard abierto con 1+ items + ESC (en step Cliente o Productos) | Aparece ConfirmDialog "¿Descartar venta en curso?" | ✅ | Playwright 23-may: ESC con Acido en carrito → confirm visible |
| 3.16 | ConfirmDialog muestra count de productos correcto | "Tienes 3 productos..." | ✅ | "Tienes **1 producto** en el carrito..." (singular correcto) |
| 3.17 | Click "Continuar venta" en confirm | Wizard sigue abierto, carrito intacto | ✅ | Wizard siguió abierto; carrito = Acido ×1 ($690) intacto en step Productos |
| 3.18 | Click "Descartar" en confirm | Wizard cierra + carrito se vacía | ✅ | Wizard cerró; carrito vacío confirmado (ver 2.9) |
| 3.19 | Wizard tras completar venta (pantalla de éxito) + ESC | Cierra sin confirm | ✅ | Code: `handleClose` cond. `!saleResult` (`pos-sale-wizard.tsx:556`) → con saleResult presente no muestra confirm |
| 3.20 | Wizard en modo complete-pending + ESC con items | NO muestra confirm (items son de la pending, no del store) | ✅ | Code: `handleClose` cond. `mode !== "complete-pending"` (`:556`) → en modo pending nunca muestra confirm |

---

## 4. POS Variant Picker

> Commit `499f9ed`. Variant picker dialog en landing para multi-variante.

### ⚙️ Backend — Code audit

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 4.1 | `pos-landing.tsx` tiene state `variantPickerProduct` | useState para producto seleccionado | ✅ | `pos-landing.tsx:98` |
| 4.2 | `handleAddProduct` filtra `has_variants && availableVariants.length > 1` antes de auto-elegir | Branch correcto | ✅ | `pos-landing.tsx:148` |
| 4.3 | `addVariantToCart` helper toma `(product, variant)` y agrega + abre wizard | Extraído del handleAddProduct original | ✅ | `pos-landing.tsx:101-135` |
| 4.4 | Variantes en picker ordenadas por SKU (usa `sortVariantsBySku`) | Llama al helper | ✅ | `pos-landing.tsx:293` |

### 🖥️ UI/UX — Picker flow

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 4.5 | Click "+" en producto 1 variante (Acido Hialuronico) | Se agrega directamente + wizard abre. NO muestra picker | ✅ | Probado en sesión (Acido → wizard step Productos auto) |
| 4.6 | Click "+" en producto multi-variante (Rimel) | Picker dialog abre mostrando todas las variantes activas | ✅ | Playwright 23-may: picker con De Luxe (M-3117 $470) + Roll-On (N-3102 $359), ordenadas por SKU |
| 4.7 | Picker muestra nombre, SKU (si aplica), precio, stock disponible por variante | Layout correcto | ✅ | `pos-landing.tsx:313-328` |
| 4.8 | Variante con stock 0 en picker → disabled + "Sin stock" rojo | No clickable | ✅ | `pos-landing.tsx:304` `disabled={isOos}` + 325 `text-red-500` |
| 4.9 | Variante con stock ya completo en carrito (qty >= stock) → disabled | No clickable | ✅ | `pos-landing.tsx:304` `!!isFull` |
| 4.10 | Click "Cancelar" en picker | Picker cierra, no se agrega nada, no abre wizard | ✅ | `pos-landing.tsx:336` |
| 4.11 | Click una variante específica | Picker cierra + producto agregado con esa variante + wizard abre | ✅ | `pos-landing.tsx:305-309` → addVariantToCart → openNewSale |
| 4.12 | Variante elegida del picker aparece correctamente en step Productos del wizard | productName + variantLabel correctos | ✅ | Playwright 23-may: elegí De Luxe → carrito "Rimel" a $470 c/u (precio de De Luxe) en step Productos |

---

## 5. Ventas KPIs

> Commit `36eff5b`. KPIs de `/ventas` reactivos al filtro de fecha. State del filtro subido al page.

### ⚙️ Backend — Code audit

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 5.1 | `useSalesStats` acepta `{ dateFrom?, dateTo? }` y los pasa a `useSales` | Filter propagation | ✅ | `queries.ts:65-69` |
| 5.2 | `getDateRange` para preset "custom" usa `T12:00:00` (no UTC midnight) | Fix de timezone aplicado | ✅ | `date-filter.ts:45` |
| 5.3 | `getRangeLabel` retorna "hoy" / "esta semana" / "mayo 2026" / "16 may 2026" | Cobertura de todos los presets | ✅ | Runtime: label "19 may 2026" |
| 5.4 | `SalesTable` es controlada via props `dateFilter` + `onDateFilterChange` | No tiene state local de filtro | ✅ | `sales-table.tsx:57-58, 61` |

### 🖥️ UI/UX — Reactividad

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 5.5 | `/ventas` carga con preset "Hoy" por default | KPIs muestran "ventas · hoy", "ingresos · hoy" | ✅ | Runtime: "ventas · hoy" / "ingresos · hoy" |
| 5.6 | Click "Esta semana" | KPIs se actualizan + subtítulos cambian a "esta semana" | ✅ | Playwright 23-may: "Hoy" (0) → "Esta semana" → Total 9 / Ingresos $9,498.26, subtítulos "esta semana" |
| 5.7 | Click "Mayo 2026" (mes actual) | KPIs muestran datos del mes + subtítulo "mayo 2026" | ✅ | Runtime: V-0047 visible al cambiar a mayo 2026 |
| 5.8 | Chevron mes anterior (Abril 2026) | KPIs y tabla cambian al mes previo | ✅ | Playwright 23-may: chevron ← → "Abril 2026", Total 0 / subtítulos "abril 2026". Chevron → (futuro) disabled correctamente |
| 5.9 | Calendario → pick fecha custom (e.g., 16 mayo 2026) | KPIs y tabla muestran datos solo del 16 mayo | ✅ | Runtime: pick 19 may → label "19 may 2026", filtro correcto |
| 5.10 | Cambiar entre presets + verificar count de tabla matches | "Total ventas" en KPI === filas filtradas en tabla con status completed | ✅ | Playwright 23-may: KPI Total=9 === tab "Ventas" Historial=9 (esta semana) |
| 5.11 | Día sin ventas (e.g., domingo viejo) | KPIs muestran 0 + "Sin ventas registradas" en tabla | ✅ | Runtime: 19 may → "No hay registros" + 0 ventas |

---

## 6. Inventario Hub

> Commit `84ef10d`. `grand_total` ahora suma solo físico + tránsito (excluye carga inicial).

### ⚙️ Backend — Code audit

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 6.1 | `useInventorySummary` calcula `grand_total = physical_total + transit_total` | Sin `initial_load_total` | ✅ | `queries.ts:431-432` |
| 6.2 | `initial_load_total` aún se retorna como campo separado | Pills siguen mostrando los 3 | ✅ | `queries.ts:429` |
| 6.3 | grep `grand_total` en src/ → solo 1 consumer (inventario hub page) | No hay break en dashboard, exports, RPCs | ✅ | Solo `page.tsx:85` |

### 🖥️ UI/UX — Hub

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 6.4 | "Valor total combinado" en hub muestra físico + tránsito | Ej: $3,053,086 + $17,580 = $3,070,666 | ✅ | Playwright 23-may: combinado $3,055,596 = Físico $3,038,016 + Tránsito $17,580 (excluye carga inicial $3,797,844) |
| 6.5 | Las 3 pills (Físico, Tránsito, Carga inicial) muestran cada total individual | Carga inicial sigue visible | ✅ | `page.tsx:177, 184, 191` |
| 6.6 | Widget "Proporción del total" en card de carga inicial calcula vs all-three | initial / (physical + transit + initial) | ✅ | Code: `initialPct = initial / (grandTotal + initial)` donde grandTotal excluye carga inicial (`inventario/page.tsx:90-91`) |
| 6.7 | Suma de las 3 pills > "Valor total combinado" (diferencia = carga inicial) | Aritmética correcta | ✅ | Playwright 23-may: 3 pills $6,853,440 − combinado $3,055,596 = $3,797,844 = carga inicial exacta |

---

## 7. Customer Networks — Backend

> Commit `939ffd2` + `499f9ed` (RPC atómico) + `5a88075` (deleted_at filter en embed).

### ⚙️ Backend — DB & RPCs

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 7.1 | Tabla `customer_networks` existe con columnas correctas | id, tenant_id, name, color, sort_order, soft delete, audit fields | ✅ | SQL: 9 columnas (incluye created_by + deleted_at) |
| 7.2 | Constraint `UNIQUE(tenant_id, name)` activo | INSERT duplicado retorna 23505 | ✅ | `customer_networks_tenant_id_name_key` |
| 7.3 | RLS habilitado + 4 policies (select/insert/update/delete) | `auth.uid() IS NOT NULL` en todas | ✅ | `rls_enabled: true, policy_count: 4` |
| 7.4 | Index `idx_customer_networks_tenant` partial WHERE deleted_at IS NULL | EXPLAIN ANALYZE | ✅ | `... WHERE (deleted_at IS NULL)` |
| 7.5 | `customers.network_id` columna nullable + FK + index | `customers_network_id_fkey` sin ON DELETE | ✅ | FK sin ON DELETE confirmed |
| 7.6 | RPC `delete_customer_network(p_network_id, p_tenant_id)` existe | Function definition | ✅ | plpgsql SECURITY DEFINER |
| 7.7 | RPC ejecuta ambos UPDATEs en una transacción | Si el segundo UPDATE falla, primero hace rollback | ✅ | Single function body (atomic) |
| 7.8 | RPC retorna `{ success: true, cleared_count: N }` | Cuenta de clientes afectados | ✅ | SQL live test: `{"success": true, "cleared_count": 2}` |
| 7.9 | RPC valida tenant ownership antes de actuar | Network de otro tenant → error "Red no encontrada" | ✅ | Code: `v_network_exists` check + early return |

### ⚙️ Backend — Server actions

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 7.10 | `createCustomerNetwork` con nombre vacío → Zod rechaza | "El nombre es requerido" | ✅ | `schemas.ts:34` `min(1, "El nombre es requerido")` |
| 7.11 | `createCustomerNetwork` con color inválido (no #RRGGBB) → Zod rechaza | Regex falla | ✅ | `schemas.ts:37` regex `^#[0-9a-fA-F]{6}$` |
| 7.12 | `createCustomerNetwork` con nombre duplicado → 23505 traducido | "Ya existe una red con el nombre..." | ✅ | `actions.ts:341-343` |
| 7.13 | `updateCustomerNetwork` requiere ID válido | `validateId` se llama primero | ✅ | `actions.ts:353` |
| 7.14 | `deleteCustomerNetwork` llama RPC (no 2 UPDATEs sequential) | Code audit | ✅ | `actions.ts:400-404` `supabase.rpc("delete_customer_network", ...)` |
| 7.15 | Todas las actions validan tenant_id | `.eq("tenant_id", TENANT_ID)` defensa en profundidad | ✅ | update/delete con `.eq("tenant_id")` |

### ⚙️ Backend — Queries

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 7.16 | `useCustomers` embebe `customer_networks(id, name, color)` con `.is("customer_networks.deleted_at", null)` | Filter aplicado al embed | ✅ | `queries.ts:26, 31` |
| 7.17 | `useCustomer(id)` también filtra deleted_at en embed | Consistency | ✅ | `queries.ts:62, 67` |
| 7.18 | `useCustomerNetworks` retorna lista + `client_count` calculado | Conteo client-side por ahora | ✅ | `queries.ts:230-247` (count map) |
| 7.19 | Eliminar red → `useCustomers` no muestra ya el nombre/color tombstoned | Verificar tras invalidate | ✅ | Garantizado por filter 7.16 + soft-delete |

---

## 8. Customer Networks — UI

### 🖥️ UI/UX — NetworkManager (Configuración)

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 8.1 | Tab "Redes" visible en `/configuracion` después de "Descuentos" | Tab pill azul | ✅ | Screenshot `networks-tab.png` |
| 8.2 | KPIs de Redes muestran: Redes definidas / Clientes clasificados / Promedio por red | 3 cards en azul | ✅ | "2 / 24 / 12" antes de crear |
| 8.3 | Empty state si no hay redes | "Sin redes definidas" + icono | ✅ | Code: `networks.length === 0` → "Sin redes definidas" (`network-manager.tsx:133,139`). No reproducible en runtime (hay 2 redes) |
| 8.4 | Click "Nueva red" → dialog con name + 8 color swatches | Picker de presets | ✅ | Screenshot `network-create-dialog.png` |
| 8.5 | Crear red "Red interna" color teal | Aparece en grid + KPI sube de 0 a 1 | ✅ | Creé "Red prueba QA" violet → KPI 2→3 |
| 8.6 | Card de red muestra nombre + color stripe + count clientes | "0 clientes" inicialmente | ✅ | Screenshot `networks-after-create.png` |
| 8.7 | Hover card de red → aparecen botones Edit + Delete | Reveal on hover | ✅ | Card tiene 2 botones (edit + delete trash) |
| 8.8 | Editar red → cambiar nombre → guardar | Card refleja cambio + invalidate | ✅ | Code: `onSubmit` branch `editId` → `updateCustomerNetwork` + toast "Red actualizada" + `invalidateQueries(["customer-networks"],["customers"])` (`network-manager.tsx:81-93`) |
| 8.9 | Eliminar red sin clientes → confirm dialog → confirmar | Card desaparece + KPI baja | ✅ | "Red prueba QA" eliminada → KPI 3→2 |
| 8.10 | Eliminar red con N clientes asignados → confirm muestra "N clientes quedará...sin red" | Plural correcto + warning | ✅ | "Red externa" → "7 clientes quedarán sin red asignada" |
| 8.11 | Eliminar red con clientes → tras confirmar, clientes ya no muestran esa red | RPC atomic + invalidate funcionan | ✅ | SQL live: cleared_count=2, customers.network_id=null |
| 8.12 | Crear red con nombre duplicado → toast error "Ya existe una red..." | Constraint feedback | ✅ | Verificado manual 23-may: toast de error aparece al duplicar nombre exacto. Cadena correcta: `actions.ts:341` (23505→`_form`) + `network-manager.tsx:85-87` (`sileo.error`) |

### 🖥️ UI/UX — Customer dialog pills

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 8.13 | Abrir cliente nuevo → expandir "Detalles adicionales" → sección "Red (opcional)" visible | Pills layout | ✅ | Screenshot `customer-detalles-expanded.png` |
| 8.14 | Sin redes definidas → muestra mensaje "No hay redes definidas. Créalas en Configuración → Redes." | Empty state amigable | ✅ | Code: mensaje exacto en `customer-dialog.tsx:467`. No reproducible en runtime (hay redes) |
| 8.15 | Con redes definidas → muestra pill "Sin red" + N pills coloreadas | Default selected = "Sin red" | ✅ | 3 pills detectadas: "Sin red", "Red externa", "Red interna" |
| 8.16 | Click pill de red → selecciona (background = color de red, texto blanco, dot blanco) | Visual feedback | ✅ | "Red externa" bg amber + color white confirmado |
| 8.17 | Click "Sin red" tras seleccionar → deselecciona | Toggle correcto | ✅ | Code: "Sin red" → `setValue("network_id", null)`, pill → `n.id`; activo por `watch("network_id")===n.id` (`customer-dialog.tsx:473-489`) |
| 8.18 | Editar cliente existente con red asignada → pill correcta pre-seleccionada | Hidrata del existingCustomer.network_id | ✅ | Adriana Emma Tostado (Red externa) → pill amber pre-selected |
| 8.19 | Guardar cliente con red asignada → toast success | Y NetworkManager actualiza count | ✅ | Code: `network_id` en el form (shouldDirty), save de cliente usa patrón estándar (toast success); count viene de `useCustomerNetworks.client_count` que recomputa al invalidar `["customers"]` |
| 8.20 | Pills layout responsive (móvil) | Wrap correcto, no overflow | ☐ | Manual mobile check |

### 🖥️ UI/UX — Tabla de clientes

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 8.21 | Columna "Red" visible en tabla `/clientes` | Después de "Descuento" | ✅ | Headers: Nombre/Tel/Email/Descuento/Red |
| 8.22 | Cliente sin red → "—" gris | Default vacío | ✅ | Filas sin red muestran "—" (screenshot) |
| 8.23 | Cliente con red → pill tinted (bg color/15, border color/40, text color sólido) | Estética coherente | ✅ | Pills tinted "Red externa" (amber), "Red interna" (teal) |
| 8.24 | Nombre de red largo en pill → truncate con title attribute | Tooltip nativo | ✅ | Code: pill con clase `truncate` + `title={net.name}` (`customer-columns.tsx:199,206`) |

---

## 9. Recibos — Paridad de descuentos

> Commit `ab0baab`. `discount_percent` per-item propagado de sale-detail/sale-detail-modal al ReceiptData. Per-product breakdown en HTML y PDF cuando difieren % entre items.

### ⚙️ Backend — Code audit

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 9.1 | `sale-detail.tsx` mapea `discount_percent: item.discount_percent != null ? Number(...) : null` | Per-item discount_percent | ✅ | `sale-detail.tsx:168` (item) + `:178` (sale) |
| 9.2 | `sale-detail-modal.tsx` aplica el mismo mapeo | Consistency | ✅ | `sale-detail-modal.tsx:149` + `:159` |
| 9.3 | HTML receipt tiene 3 branches: cart-only / sameForAll / per-product | + cart-level-extra | ✅ | `sale-receipt.tsx`: `withDiscount.length===0` (260), sameForAll (116), per-product map (299), `hasCartLevelExtra` line (321) |
| 9.4 | PDF receipt mismo control flow | Mirror de HTML | ✅ | `sale-receipt-pdf.ts`: branch 218, per-product 243, cartLevelExtra 124/262 — espejo del HTML |

### 🖥️ UI/UX — Recibo on-screen (sale detail panel)

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 9.5 | Venta con 2 productos: uno con 40%, otro regalo (100%) → sale detail muestra breakdown | "% X (40%) -$Y" + "🎁 Z (100%) REGALO -$W" | ✅ | Playwright 23-may V-0048: footer "% Aceite (40%) -$296.00" + "★ Acido (100%) Regalo -$690.00", Total $444.00 |
| 9.6 | Venta con 1 producto, 40% descuento → muestra "Descuento (40%) -$X" agregado | sameForAll branch | ✅ | Code: branch sameForAll verificado en 10.18 (`sale-receipt.tsx:274`) |
| 9.7 | Venta sin descuento → footer no muestra línea de descuento | discountAmount = 0 | ✅ | Code: branch solo agrega línea si hay descuento (9.3); discountAmount=0 → sin línea |

### 🖥️ UI/UX — Recibo HTML (impresión)

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 9.8 | Imprimir V-0048 → ticket muestra "-40%" inline en Aceite Omega + "-100% REGALO" en Acido | Per-item visible | ✅ | Contenido verificado en PDF (espejo del HTML, 9.3/9.4) + on-screen: "-40%" / "-100% GRATIS". Estilo térmico → §10 |
| 9.9 | Footer del ticket impreso muestra "% Aceite (40%) -$296.00" + "Acido (100%) REGALO -$690.00" | Breakdown | ✅ | PDF + on-screen: "% Aceite (40%) -$296.00" + "★ Acido (100%) REGALO -$690.00" |
| 9.10 | V-0048 footer suma correctamente al TOTAL $444.00 | Subtotal $1430 - $986 = $444 | ✅ | PDF + on-screen: $1,430 − $296 − $690 = $444.00 |
| 9.11 | Venta sin descuento → ticket sin línea de descuento | Gracioso degrade | ✅ | Code: branch sin descuento no agrega línea (9.3) |

### 🖥️ UI/UX — Recibo PDF

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 9.12 | Descargar PDF V-0048 → mismo breakdown que HTML | Paridad funcional | ✅ | Playwright 23-may: `Recibo-V-0048.pdf` con el mismo breakdown que on-screen |
| 9.13 | Iconos en PDF: `%` o `★` (no emoji 🎁 que no renderiza) | ASCII safe | ✅ | PDF: "%" para 40%, "★" para REGALO — sin emoji |
| 9.14 | Color del icono REGALO en PDF: violet (#8b5cf6) | Visual distinción | ✅ | PDF: línea REGALO + "GRATIS" en violeta |

---

## 10. Recibo Térmico

> Commits `17a1cbe` + `56a212e` + `423e016` + `5a88075`. Rediseño B&W monospace + logos B&W + logo pequeño + bug cart-level fix.

### ⚙️ Backend — Code audit

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 10.1 | `sale-receipt.tsx` usa `FONT_FAMILY = 'JetBrains Mono', 'Menlo', 'Courier New', ...` | Monospace | ✅ | `sale-receipt.tsx:47-48` |
| 10.2 | Color de texto en estilos = `#000` (no `#1a1a1a` ni colores tintados) | Black on white | ✅ | Único color de texto = `#000` (`sale-receipt.tsx:127`); sin `#1a1a1a` |
| 10.3 | No hay `backgroundColor` distinto de `#fff` (sin fondos rosa/gris) | Thermal-safe | ✅ | grep: 0 declaraciones de `backgroundColor` (fondo blanco por defecto) |
| 10.4 | No hay emojis Unicode (🎁, 📍, etc.) | Compat caracteres | ✅ | Sin emoji Unicode; usa texto "REGALO"/"GRATIS" |
| 10.5 | `public/EclatLogo_Black.svg` existe + todos los `fill` son `#000000` | grep | ✅ | Existe; grep: 0 fills no-negros |
| 10.6 | `public/abbrixLogo.svg` ya era 100% negro | grep | ✅ | Existe; fills `fill="black"` (100% negro) |
| 10.7 | Logo del header con `width: "38mm"` (no 62mm anterior) | Tamaño correcto | ✅ | `sale-receipt.tsx:143` `width: "38mm"`; sin 62mm |

### 🖥️ UI/UX — Visual del ticket

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 10.8 | Header del ticket: logo Eclat B&W + dirección + teléfono | Centrado | ☐ | |
| 10.9 | Logo tamaño ~38mm no roba la mitad del ancho | Proporcionado | ☐ | |
| 10.10 | Separadores con dashes (------) en gris (no líneas de color) | Visual coherente | ☐ | |
| 10.11 | FOLIO + Fecha + CLIENTE como rows con bold contrast | Jerarquía | ☐ | |
| 10.12 | Items: nombre en bold + "1 x $XXX -40%" en regular + GRATIS para regalos | Weight contrast | ☐ | |
| 10.13 | TOTAL grande en bold (13px) | Highlight | ☐ | |
| 10.14 | Footer: "GRACIAS POR TU COMPRA" + "Powered by [logo eclat 12px]" + "Desarrollado por [logo abbrix 10px]" | Logos inline | ☐ | |
| 10.15 | Impresión real en térmica → todo se ve oscuro y nítido | No gris desvanecido | ☐ | Requiere papel térmico físico |

### 🖥️ UI/UX — Bug cart-level

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 10.16 | V-0047 (descuento custom_amount $469.95, sin per-item) → ticket muestra "Descuento (99.99%) -$469.95" | Línea única, no header vacío | ✅ | DOM inspect: "Subtotal $470.00 / Descuento (99.99%) -$469.95 / TOTAL $0.05" |
| 10.17 | Venta sin per-item discount pero con custom_amount → render correcto | Mismo fix | ✅ | Branch A (`withDiscount.length === 0`) `sale-receipt.tsx:260` |
| 10.18 | Venta con per-item discounts iguales → "Descuento (X%) -$Y" agregada | sameForAll branch | ✅ | Branch B `sale-receipt.tsx:274` |
| 10.19 | Venta con per-item discounts distintos → header "DESCUENTOS" + 1 línea por producto | Per-product branch | ✅ | Branch C `sale-receipt.tsx:288-329` |

---

## 11. Recibo PDF

> Commits `e2623f7` + `78cc2ce`. Tamaño carta + centered narrow + overflow fix + italic crash fix.

### ⚙️ Backend — Code audit

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 11.1 | `Page` usa `size: "LETTER"` (no `[W, pageHeight]` dynamic) | Letter standard | ✅ | `sale-receipt-pdf.ts:130` |
| 11.2 | `PAGE_H_PAD = 136` y `PAGE_V_PAD = 60` (centra contenido en ~340pt) | Centered narrow | ✅ | `sale-receipt-pdf.ts:48-49` |
| 11.3 | No hay función `estimateHeight` ni dynamic page height | Limpio | ✅ | grep limpio |
| 11.4 | `clientName` style tiene `flexShrink: 1` y `textAlign: "right"` | Overflow handling | ✅ | `sale-receipt-pdf.ts:72` |
| 11.5 | Referencia de pago NO usa `fontStyle: "italic"` | PlusJakarta sin variante italic registrada | ✅ | Línea 286 comentario explícito |

### 🖥️ UI/UX — Visual del PDF

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 11.6 | Descargar PDF de V-0047 → archivo en 1 sola página (no 2da en blanco) | Single page | ✅ | `pdfinfo`: Pages: 1 |
| 11.7 | Dimensiones del PDF = Carta (612 x 792 pt = 8.5 x 11 in) | pdfinfo output | ✅ | `pdfinfo`: 612 x 792 pts (letter) |
| 11.8 | Contenido centrado con margen blanco a ambos lados | Visual narrow column | ✅ | Render `.playwright-mcp/Recibo-V-0047-1.png` |
| 11.9 | "MARIA DEL ROSARIO CORTEZ BORCEGUIN" se ve completo, sin overflow | El bug original | ✅ | Render PDF: nombre completo a la derecha |
| 11.10 | Diseño colorido preservado: accent rosa, total con bg rosa, etc. | Aesthetic match | ✅ | Render: línea separadora rosa + TOTAL bg rosa |
| 11.11 | PDF con pago de transferencia + referencia "BANCOPEL A BX" → NO crashea | Italic fix | ✅ | V-0047 (transferencia + ref) generó OK |
| 11.12 | Referencia se ve gris e indented (sin italic pero diferenciado) | Visual hierarchy | ✅ | Render: "Ref: BANCOPEL A BX" gris indented |
| 11.13 | PDF con descuento custom_amount → línea única "Descuento (X%)" (no header vacío) | Cart-level fix | ✅ | Render: "Descuento (99.99%) -$469.95" línea única rosa |
| 11.14 | PDF con regalo + descuento mixto → per-product breakdown con cart-level extra al final | Edge fix | ✅ | Playwright 23-may `Recibo-V-0048.pdf`: per-product (Aceite 40% + Acido 100% regalo) renderiza OK. (V-0048 sin cart-level extra; rama cart-level cubierta por 11.13) |

---

## 12. Cross-feature integration

### ⚙️ Backend — Data integrity

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 12.1 | Eliminar una red asignada a clientes → clientes ahora tienen `network_id IS NULL` | SQL query confirma | ✅ | Probado live en 7.8 (`cleared_count=2`) + 8.11 (`customers.network_id=null`) |
| 12.2 | Eliminar red → tabla cliente UI muestra "—" en columna Red | Consistency UI / DB | ✅ | Composición: 8.11 (RPC pone `network_id=null` en clientes afectados) + 8.22 (cliente sin red muestra "—") |
| 12.3 | Crear venta pendiente con descuento → completar → recibo HTML muestra descuento correcto | End-to-end | ☐ | Cubierto por composición (3.6 + 9.x); e2e en vivo pendiente (mutación) |
| 12.4 | Crear venta pendiente con descuento → completar → recibo PDF muestra descuento correcto | End-to-end | ☐ | Cubierto por composición (3.13 + 9.12); e2e en vivo pendiente (mutación) |
| 12.5 | Sales KPIs reaccionan tras crear/cancelar venta del día | Query invalidation correcta | ☐ | KPIs date-reactivos verificados (§5); invalidación post-mutación es patrón establecido — no disparado en vivo |
| 12.6 | Pendientes-list refresca tras completar pendiente | El fix `["pos-pending-sales"]` funciona | ☐ | Requiere flujo de pendiente en vivo (mutación) |

### 🖥️ UI/UX — Cross-tab consistency

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 12.7 | Tab A en `/clientes`, Tab B borra red → Tab A refresh → red desaparece de la pill | Invalidate + filter | ☐ | Cross-tab: requiere 2 pestañas — spot-check del usuario |
| 12.8 | Tab A en `/configuracion → Redes`, Tab B asigna red a cliente → Tab A NO actualiza count automáticamente (no realtime) | Manual refresh OK | ☐ | Conocido, no realtime. Cross-tab — spot-check del usuario |
| 12.9 | Tab A en `/pos`, Tab B borra un producto → Tab A actualiza grid (realtime sync) | Sin regresión | ☐ | Cross-tab realtime: requiere 2 pestañas — spot-check del usuario |

---

## 13. Edge cases & Regressions

### ⚙️ Backend — Edge

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 13.1 | Custom date filter el 1ro de enero en TZ negativo → query devuelve datos del 1 ene local (no 31 dic) | Timezone fix verificado | ✅ | Runtime: pick 19 may → label "19 may 2026" (no shift); `date-filter.ts:45` `T12:00:00` |
| 13.2 | Venta con `sale_items.discount_percent = NULL` (legacy data) + discount > 0 → recibo no rompe | Backward compat | ✅ | Null-safe: `Number(i.discount_percent ?? 0)` (HTML `:106,300` / PDF `:118`); `withDiscount` filtra por `discount > 0` (monto, no %) |
| 13.3 | Venta sin items (carrito vacío) → wizard no permite avanzar a Pago | Validación existente | ✅ | Botón "Continuar" del paso Productos `disabled={!hasItems}` (`wizard-products-step.tsx:585`) |
| 13.4 | Crear red con nombre 100+ chars → Zod rechaza | max(100) | ✅ | `schemas.ts:34` `name: z.string().min(1...).max(100)` |
| 13.5 | Asignar `network_id` a cliente cuya red fue eliminada (race) → cliente queda con NULL al cargarse | Filtro de deleted_at en embed | ✅ | Embed `.is("customer_networks.deleted_at", null)` (7.16/7.17) → red tombstoned no se hidrata; RPC además setea NULL |
| 13.6 | `delete_customer_network` con UUID inválido → action `validateId` rechaza antes de RPC | Security layer | ✅ | `actions.ts` `deleteCustomerNetwork`: `validateId(id)` es la 1ª línea, antes del RPC |
| 13.7 | Wizard cerrado con ESC durante submit (`submittingRef.current = true`) → confirm NO se muestra, espera | Race protection | ✅ | `pos-sale-wizard.tsx:553-554` `handleClose` retorna temprano si `submittingRef.current`; Dialog `onOpenChange:568` también |

### 🖥️ UI/UX — Regression sweep

| # | Test | Expected | Status | Notes |
|---|------|----------|--------|-------|
| 13.8 | POS funciona end-to-end: agregar producto → cliente → pago efectivo → confirmar | Flow completo | ☐ | Parcial: flujo ejercitado hasta paso Pago sin errores (§2-4). Pago/confirmar NO completado para no mutar DB — pendiente run transaccional en vivo |
| 13.9 | Crear vale desde POS sigue funcionando | Sin regresión | ☐ | `/vales` carga sin errores; crear vale = mutación, pendiente run en vivo |
| 13.10 | Devolución sigue funcionando (return-dialog) | Sin regresión | ☐ | sale-detail carga sin errores; crear devolución = mutación, pendiente run en vivo |
| 13.11 | Notas de crédito (préstamo/intercambio) siguen funcionando | Sin regresión | ☐ | `/notas-credito` carga sin errores; crear nota = mutación, pendiente run en vivo |
| 13.12 | Exports Excel + PDF siguen generando correctamente | Sin regresión | ✅ | `/reportes` carga sin errores; exports ya verificados en `TEST-PLAN.md` 13.13-13.20. PDF de recibo verificado hoy (9.12) |
| 13.13 | Imprimir ticket desde sale-detail-modal funciona | Click "Imprimir" abre print dialog | ☐ | No capturable vía Playwright (window.print nativo) — spot-check del usuario |
| 13.14 | Descargar PDF desde sale-detail-modal funciona | Click "PDF" descarga archivo | ✅ | PDF descargado OK desde sale-detail (`Recibo-V-0048.pdf`); el modal usa el mismo componente SaleReceiptPDF |
| 13.15 | Navegación entre módulos (sidebar) sin errores | No 404, no flashes | ✅ | Playwright 23-may: 11 rutas navegadas (pos/ventas/ventas[id]/config/inventario/vales/notas/reportes/productos/clientes/ayuda) — 0 errores de consola en todas |
| 13.16 | Filtros URL state (nuqs) persisten en `/ventas` tras refresh | `q`, `status` params survive | ✅ | Playwright 23-may: cargar `/ventas?status=completed` directo hidrata tab "Ventas" + tabla filtrada. `q` usa mismo patrón nuqs |

---

## Summary

| Sección | ⚙️ Backend | 🖥️ UI/UX | Total |
|---------|-----------|---------|-------|
| 1. Variant Sorting | 3 | 8 | 11 |
| 2. POS Landing | 4 | 9 | 13 |
| 3. POS Sale Wizard | 7 | 13 | 20 |
| 4. POS Variant Picker | 4 | 8 | 12 |
| 5. Ventas KPIs | 4 | 7 | 11 |
| 6. Inventario Hub | 3 | 4 | 7 |
| 7. Customer Networks (Backend) | 15 | 0 | 15 |
| 8. Customer Networks (UI) | 0 | 24 | 24 |
| 9. Recibos paridad | 4 | 10 | 14 |
| 10. Recibo térmico | 7 | 12 | 19 |
| 11. Recibo PDF | 5 | 9 | 14 |
| 12. Cross-feature | 6 | 3 | 9 |
| 13. Edge cases | 7 | 9 | 16 |
| **Total** | **69** | **116** | **185** |

---

## Cómo correr este plan

**Backend (⚙️)**: Claude puede ejecutar la mayoría vía code audit + SQL queries via MCP. Las marcadas como "Code audit" requieren leer archivos; las "SQL verified" requieren ejecutar consultas vía `mcp__supabase__execute_sql`.

**UI/UX (🖥️)**: combinación de Playwright automatizado + verificación manual:
- Flows simples (cargar página, click, screenshot): Playwright
- Validaciones visuales (color, weight, layout): manual ojo humano
- Impresión real térmica (10.15): requiere papel + impresora térmica conectada

**Orden sugerido para hoy:**
1. Sections 1-4 (POS — lo más complejo y donde más cambió)
2. Section 7-8 (Networks — feature nueva)
3. Section 9-11 (Recibos — flujo crítico para el negocio)
4. Section 5-6 (Quick wins — verificación rápida)
5. Section 12-13 (Integration + edge cases — al final)

Empieza con las celdas críticas P0 implícitas:
- **3.10, 3.11** — el bug original del punto 2 que arreglamos
- **8.11** — borrado atómico de red
- **10.16, 11.13** — el bug de descuento cart-level
- **11.6, 11.9, 11.11** — los 3 fixes del PDF
- **13.1** — timezone fix (afecta datos)
